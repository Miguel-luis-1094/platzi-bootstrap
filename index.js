/* esta funcion se hace para que al poner el cursor en el init se active y aparezca un mensaje, esto gracias a jquery que viene con bootstrap */

$(function() {
    $('[data-toogle="tooltip"]').tooltip()
})
